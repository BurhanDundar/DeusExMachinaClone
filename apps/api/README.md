# API application boundary

Reserved for a future catalog/order service. The storefront currently consumes typed mock repositories from `apps/storefront/src/data`; replacing those modules with API clients will not change presentation components.
