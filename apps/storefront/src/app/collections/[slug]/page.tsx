import {products} from "@/data/products";import {ProductCard} from "@/components/commerce/ProductCard";import {Footer} from "@/components/layout/Footer";
export default async function Collection({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const title=slug.split("-").map(x=>x[0]?.toUpperCase()+x.slice(1)).join(" ");return <main><section className="shell pb-20 pt-12"><div className="mb-12 flex flex-wrap items-end justify-between gap-5"><div><p className="text-sm text-black/50">Collection / {products.length} products</p><h1 className="display mt-2 text-5xl md:text-7xl">{title}</h1></div><div className="flex gap-2"><button className="border border-black px-5 py-3">Filter ＋</button><button className="border border-black px-5 py-3">Sort ↓</button></div></div><div className="grid grid-cols-2 gap-x-2 gap-y-12 md:grid-cols-4 md:gap-x-3">{products.map(p=><ProductCard product={p} key={p.id}/>)}</div></section><Footer/></main>}
