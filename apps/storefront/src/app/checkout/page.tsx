"use client";

import Image from "next/image";
import Link from "next/link";
import { FormEvent, useMemo, useState } from "react";
import { Check, CircleHelp, LockKeyhole, Search } from "lucide-react";
import { useUIStore } from "@/store/ui-store";

const inputClass = "mt-2 h-12 w-full rounded-xl border border-black/15 bg-white px-3 text-sm outline-none placeholder:text-black/55 focus:border-black";

function Field({ label, className = "", children }: { label?: string; className?: string; children: React.ReactNode }) {
  return <label className={`block ${className}`}>{label && <span className="sr-only">{label}</span>}{children}</label>;
}

export default function CheckoutPage() {
  const items = useUIStore(state => state.items);
  const [payment, setPayment] = useState<"card" | "paypal" | "klarna">("card");
  const [terms, setTerms] = useState(false);
  const [notice, setNotice] = useState("");
  const subtotal = useMemo(() => items.reduce((total, item) => total + item.product.price * item.quantity, 0), [items]);
  const count = items.reduce((total, item) => total + item.quantity, 0);

  function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    setNotice("Online payments are not connected yet. Your card details have not been sent or saved.");
  }

  if (!items.length) return <main className="mx-auto max-w-xl px-5 py-20"><h1 className="display text-5xl">Your bag is empty</h1><p className="mt-4 text-black/60">Add something to your bag before checking out.</p><Link className="focus-ring mt-8 inline-block bg-black px-6 py-4 font-bold text-white" href="/">Continue shopping</Link></main>;

  return <main className="bg-white lg:grid lg:grid-cols-2"><form onSubmit={submit} className="mx-auto w-full max-w-[540px] px-5 py-10 sm:px-8 lg:px-0 lg:py-10"><div className="space-y-9"><section><p className="mb-4 text-center text-sm text-black/55">Express checkout</p><div className="grid grid-cols-3 gap-2"><button type="button" className="h-12 rounded-xl bg-[#5a31f4] text-lg font-bold text-white">shop</button><button type="button" className="h-12 rounded-xl bg-[#ffc439] text-lg font-bold italic text-[#003087]">PayPal</button><button type="button" className="h-12 rounded-xl bg-black font-bold text-white"><span className="text-[#4285f4]">G</span> Pay</button></div><div className="my-7 flex items-center gap-4 text-xs text-black/55 before:h-px before:flex-1 before:bg-black/15 after:h-px after:flex-1 after:bg-black/15">OR</div></section><section><div className="mb-3 flex items-center justify-between"><h1 className="text-xl font-bold">Contact</h1><Link className="underline" href="/account/login">Sign in</Link></div><Field label="Email"><div className="relative"><input className={inputClass} type="email" autoComplete="email" placeholder="Email" required /><CircleHelp className="absolute right-3 top-5 text-black/55" size={16}/></div></Field><label className="mt-3 flex items-center gap-2 text-sm"><input className="size-[19px] appearance-none rounded-md border border-black/15 checked:border-black checked:bg-black" type="checkbox" /><Check className="-ml-[22px] pointer-events-none text-white" size={14}/><span className="ml-1">Email me with news and offers</span></label></section><section><h2 className="mb-3 text-xl font-bold">Delivery</h2><div className="grid grid-cols-2 gap-3"><Field label="First name"><input className={inputClass} autoComplete="given-name" placeholder="First name" required /></Field><Field label="Last name"><input className={inputClass} autoComplete="family-name" placeholder="Last name" required /></Field></div><Field label="Address" className="mt-3"><div className="relative"><input className={inputClass} autoComplete="street-address" placeholder="Address" required /><Search className="absolute right-3 top-5 text-black/55" size={16}/></div></Field><div className="mt-3 grid grid-cols-2 gap-3"><Field label="Postal code"><input className={inputClass} autoComplete="postal-code" placeholder="Postal code" required /></Field><Field label="City"><input className={inputClass} autoComplete="address-level2" placeholder="City" required /></Field></div><Field label="Phone" className="mt-3"><div className="relative"><input className={inputClass} type="tel" autoComplete="tel" placeholder="Phone" required /><CircleHelp className="absolute right-3 top-5 text-black/55" size={16}/></div></Field></section><section><h2 className="text-xl font-bold">Shipping method</h2><div className="mt-3 rounded-xl bg-black/[.045] px-5 py-4 text-center text-sm text-black/55">Enter your shipping address to view available shipping methods.</div></section><section><h2 className="text-xl font-bold">Payment</h2><p className="mt-1 text-sm text-black/55">All transactions are secure and encrypted.</p><div className="mt-3 overflow-hidden rounded-xl border border-black"><label className="flex h-13 items-center justify-between border-b border-black px-4 py-3"><span className="flex items-center gap-3 font-semibold"><input checked={payment === "card"} onChange={() => setPayment("card")} type="radio" name="payment" />Credit card</span><span className="flex gap-1 text-[10px] font-bold"><i className="rounded bg-blue-700 px-1 py-0.5 not-italic text-white">VISA</i><i className="rounded bg-red-500 px-1 py-0.5 not-italic text-white">MC</i></span></label>{payment === "card" && <div className="bg-black/[.035] p-4"><Field label="Card number"><div className="relative"><input className={inputClass} placeholder="Card number" inputMode="numeric" /><LockKeyhole className="absolute right-3 top-5 text-black/55" size={16}/></div></Field><div className="mt-3 grid grid-cols-2 gap-3"><Field label="Expiry date"><input className={inputClass} placeholder="Expiration date (MM / YY)" inputMode="numeric" /></Field><Field label="Security code"><input className={inputClass} placeholder="Security code" inputMode="numeric" /></Field></div><Field label="Name on card" className="mt-3"><input className={inputClass} placeholder="Name on card" /></Field><label className="mt-4 flex items-center gap-2 text-sm"><input type="checkbox" defaultChecked />Use shipping address as billing address</label></div>}<label className="flex items-center justify-between border-t border-black/15 px-4 py-4"><span><input checked={payment === "paypal"} onChange={() => setPayment("paypal")} type="radio" name="payment" /> <b className="ml-2">PayPal</b></span><b className="italic text-[#003087]">PayPal</b></label><label className="flex items-center justify-between border-t border-black/15 px-4 py-4"><span><input checked={payment === "klarna"} onChange={() => setPayment("klarna")} type="radio" name="payment" /> <b className="ml-2">Pay with Klarna</b></span><b className="rounded bg-pink-300 px-1 text-xs">Klarna</b></label></div></section><section><label className="flex items-start gap-2 text-sm"><input checked={terms} onChange={event => setTerms(event.target.checked)} className="mt-0.5" type="checkbox" required />I have read and agree to the <a className="underline" href="#terms">Terms of Service.</a></label><div className="mt-8 flex items-start justify-between gap-4 text-sm"><p><b>Save my information for a faster checkout</b><br/><span className="text-black/55">By paying, you agree to create a Shop account subject to Shop&apos;s Terms and Privacy Policy.</span></p><button type="button" className="shrink-0">Not now</button></div><button className="mt-8 w-full rounded-xl bg-[#202020] py-4 font-bold text-white disabled:cursor-not-allowed disabled:bg-black/20" disabled={!terms} type="submit">Pay now</button>{notice && <p className="mt-3 rounded-lg bg-amber-50 p-3 text-sm text-amber-950" role="status">{notice}</p>}<div className="mt-10 flex gap-5 border-t border-black/15 pt-4 text-sm underline"><a href="#refund">Refund policy</a><a href="#privacy">Privacy policy</a><a id="terms" href="#terms">Terms of service</a></div></section></div></form><OrderSummary items={items} subtotal={subtotal} count={count} /></main>;
}

function OrderSummary({ items, subtotal, count }: { items: ReturnType<typeof useUIStore.getState>["items"]; subtotal: number; count: number }) {
  return <aside className="border-t border-black/10 bg-[#f4f4f4] lg:sticky lg:top-[80px] lg:h-[calc(100vh-80px)] lg:border-l lg:border-t-0"><div className="mx-auto flex h-full w-full max-w-[540px] flex-col px-5 py-8 sm:px-8 lg:mx-0 lg:px-10"><div className="max-h-[310px] overflow-y-auto pr-2 [scrollbar-color:#999_transparent] [scrollbar-width:thin]">{items.map(item => <div className="grid grid-cols-[64px_1fr_auto] items-center gap-3 py-2" key={item.product.id + item.size}><div className="relative aspect-square overflow-hidden rounded-xl border border-black/15 bg-white"><Image src={item.product.images[0]} alt={item.product.name} fill className="object-cover" /><span className="absolute -right-1 -top-1 grid size-5 place-items-center rounded-full bg-black text-xs text-white">{item.quantity}</span></div><div><p className="font-medium">{item.product.name}</p><p className="text-sm text-black/55">{item.product.color} / {item.size}</p></div><p>€{(item.product.price * item.quantity).toFixed(2)}</p></div>)}</div><div className="mt-5 flex gap-3"><input className="h-12 min-w-0 flex-1 rounded-xl border border-black/15 bg-white px-3 outline-none focus:border-black" placeholder="Discount code or gift card" /><button className="rounded-xl border border-black/10 px-4 font-semibold text-black/45" type="button">Apply</button></div><div className="mt-8 space-y-2 text-sm"><div className="flex justify-between"><span>Subtotal · {count} items</span><span>€{subtotal.toFixed(2)}</span></div><div className="flex justify-between"><span>Shipping</span><span className="text-black/55">Enter shipping address</span></div><div className="flex items-baseline justify-between pt-2 text-lg font-bold"><span>Total</span><span><small className="mr-1 text-xs font-normal text-black/55">EUR</small>€{subtotal.toFixed(2)}</span></div></div></div></aside>;
}
